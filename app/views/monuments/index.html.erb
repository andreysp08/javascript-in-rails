<p style="color: green"><%= notice %></p>

<h1>Monuments</h1>
<div class="col" data-controller="insert-in-list">
  <div id="monuments" data-insert-in-list-target="items">
    <% @monuments.each do |monument| %>
      <%= render monument %>
      <p>
        <%= link_to "Show this monument", monument %>
      </p>
    <% end %>
  </div>
</div>
<%= link_to "New monument", new_monument_path %>

<div data-controller="confetti" class="mt-5">
  <button data-action="click->confetti#confetti" class="btn btn-outline-primary">Confetti</button>
</div>


<div data-controller="toggle" class="mt-5">
  <button data-action="click->toggle#fire" class="btn btn-outline-primary">Add a new monument</button>
  <div data-toggle-target="togglableElement" class="d-none">
    <%= render "form", monument: @monument %>
  </div>
</div>

